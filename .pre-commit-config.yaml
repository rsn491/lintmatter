repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --all
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-clippy
        name: cargo clippy
        entry: cargo clippy --workspace --all-targets -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-check
        name: cargo check
        entry: cargo check --workspace --all-targets
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-test
        name: cargo test
        entry: cargo test --workspace
        language: system
        types: [rust]
        pass_filenames: false

      - id: lintmatter-self
        name: lintmatter (self-lint)
        # Excludes come from the repository's .lintmatter.yaml; --strict is a
        # run-behavior flag, so it isn't configurable via that file.
        entry: cargo run --quiet --bin lintmatter -- --strict .
        language: system
        always_run: true
        pass_filenames: false
