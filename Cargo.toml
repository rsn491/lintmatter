[workspace]
members = ["web"]

[package]
name = "lintmatter"
version = "0.1.0"
publish = ["crates-io"]
edition = "2024"
rust-version = "1.93"
description = "A linter for agent instruction files: AGENTS.md, SKILL.md, and friends."
repository = "https://github.com/rsn491/lintmatter"
homepage = "https://github.com/rsn491/lintmatter"
license = "MIT"

[lib]
name = "lintmatter"
path = "src/lib.rs"

[[bin]]
name = "lintmatter"
path = "src/main.rs"

[dependencies]
saphyr = "0.0.11"
regex = "1"
glob = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[profile.release]
lto = true

[profile.dist]
inherits = "release"
lto = "thin"

# Config for 'dist'
[workspace.metadata.dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.33.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "homebrew", "npm"]
# Publish prerelease tags to crates.io as prerelease versions too
publish-prereleases = true
# The crates.io publisher authenticates through GitHub OIDC
github-custom-job-permissions = { "publish-crate" = { contents = "read", id-token = "write" } }
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# A GitHub repo to push Homebrew formulas to
tap = "rsn491/homebrew-tap"
# Publish jobs to run in CI
publish-jobs = ["homebrew", "npm", "./publish-crate"]
